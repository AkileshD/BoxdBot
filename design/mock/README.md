# design/mock/

Place user-approved UI sketches here before any UI code is written.

See `design/ui-requirements.md` for what the UI must contain.
See SPEC.md (§0, design/ section) for the full workflow.
